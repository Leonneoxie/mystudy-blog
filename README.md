# MyStudy 技术博客

这是由 MyStudy 本地学习系统导出的独立静态博客，只包含公开文章、页面和必要样式。

## 发布

1. 在 GitHub 仓库的 Settings → Pages 中选择 **GitHub Actions** 作为 Source。
2. 推送到 `main`，或从 Actions 手动运行 `Deploy GitHub Pages`。
3. 如需自定义域名，请先在 GitHub Pages 设置中验证并配置域名。

`site/` 是可直接部署的静态产物；文章源与完整课程不包含在此公开仓库中。
